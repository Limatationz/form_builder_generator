name: form_builder_generator
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_form_builder: ^9.1.1
  form_builder_validators: ^9.1.0
  nameof_annotation:
    path: ../nameof_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
